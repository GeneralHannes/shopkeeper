# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# --- Environment / secrets ---
.env
.env.*
!.env.example

# --- Postgres (Docker volume data lives in-project) ---
db/data/
*.dump
*.sql.bak
# Portable data dumps: carried by Syncthing between machines, but NOT committed to git
# (may be large / contain real shop data). See scripts/db-export.sh.
db/dumps/

# --- Ollama / models (large; never commit) ---
models/
*.gguf
*.bin

# --- Tooling ---
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
